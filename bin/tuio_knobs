#!/usr/bin/env ruby
require 'rubygems'

$:.unshift( File.join( File.dirname( __FILE__ ), '..', 'lib') )

require 'tuio_midi_knobs'

puts "starting midi knobs..."
TuioMidiKnobs.new.start
puts "running midi knobs..."
sleep